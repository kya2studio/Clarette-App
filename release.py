"""Single release identity for runtime, native windows and packaging."""
VERSION = '1.0.1'
BUILD = '1001'
TITLE = 'Clarette · Headshot Workflow'
# Where the "Buy a License" button sends people. This is baked into the app
# at build time on purpose -- a per-user setting can't reach anyone else's
# install, so every copy you distribute needs this set here before building.
PURCHASE_URL = ''
